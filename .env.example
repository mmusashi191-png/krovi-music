# Browser build-time configuration
# Local development: ws://localhost:8787/ws
# Hosted production: the Render WebSocket endpoint, for example wss://your-service.onrender.com/ws
VITE_CONNECT_WS_URL=

# Server runtime configuration
# YOUTUBE_API_KEY is configured in the Render service, not in the browser.