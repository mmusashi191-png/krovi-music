# Frontend build-time configuration
# Local development can leave both blank.
VITE_CONNECT_WS_URL=
VITE_API_BASE_URL=

# Server runtime configuration
# YOUTUBE_API_KEY belongs on the Node/Render service, never in the browser.
