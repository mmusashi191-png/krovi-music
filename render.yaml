services:
  - type: web
    name: krovi-music-api
    runtime: node
    branch: rebuild/professional-foundation
    autoDeployTrigger: commit
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false
